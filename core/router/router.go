package router

//A router is a special type of actor whose job is to route messages to other actors called routees.
//Different routers use different strategies to route messages efficiently.

//router是一种特殊类型的actor，其工作是将消息路由到称为 routees 的其他参与者。
//IRouter 定义了所有router规范， 每种router使用不同的策略来有效地路由消息
